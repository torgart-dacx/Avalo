# Avalo RPG — журнал обновлений

Дата — что изменили (объём). Новые записи сверху. Ведём для отслеживания процесса разработки.

---

## 2026-07-05 (3) — Мобильный фикс: ловушка «поверни телефон» + адресная строка

- **🐞 Ловушка автоповорота**: портрет-медиазапрос ПРЯТАЛ канвас (`canvas{display:none}`) → на телефоне с ВЫКЛюченным автоповоротом `orientation:portrait` не менялся даже в ландшафте, игрок навсегда застревал на плашке. Фикс: канвас в портрете больше не прячется — игра всегда видима и играбельна (letterbox по ширине); плашка «поверни» → компактный неблокирующий нижний баннер.
- **📱 Адресная строка браузера**: по первому касанию/кнопке «⛶ Во весь экран» — `requestFullscreen()` + `screen.orientation.lock('landscape')` (Android: прячет URL-бар и разворачивает в ландшафт). iOS Safari эти API не поддерживает — там игра остаётся играбельной в портрете; полный экран — через «Добавить на экран Домой» (PWA-мета уже есть).

## 2026-07-05 (2) — Магазин косметических эмблем архетипов (9 процедурных иконок, 500🪙)

Снапшот: `versions/index_2026-07-05_arch-emblems.html`. Архетипы остаются **бесплатными** (выбор на 15 ур. как был) — за золото продаётся только косметический вид иконки.

- **9 процедурных эмблем архетипов** (`ARCH_MOTIF`/`archEmblemRender`/`drawArchEmblem`, реюз avGlow/avPoly/shade/_rgba — ноль ассетов, всё в одном файле): октагональная кристалл-рамка + радиальные лучи + ореол + детальный мотив под каждый архетип (Берсерк — скрещённые топоры+кровь, Дуэлянт — рапиры+фантом, Страж — щит с крестом, Стихии — сфера огонь/лёд, Аркан — вихрь-руна, Призыватель — дух-череп в круге призыва, Снайпер — мишень+стрела, Ловчий — след лапы+клык, Шквал — вихрь клинков). Кэш в offscreen-канвас.
- **Магазин** — новая вкладка «⭐ Эмблемы» в кузнице (сетка 3×3 по классам, крупные сияющие ячейки, залоченные затемнены с ценой 500🪙, купленные горят с ✓, hover-инфо). Покупка — `meta.archIcons={key:1}`, списывает золото, `saveSave`.
- **Отображение**: купленная эмблема заменяет эмодзи-кружок на карточке выбора архетипа (15 ур.) и рисуется печатью-пином на HUD-портрете во время игры этим архетипом.
- Кузница: таб-бар переразложен на 4 вкладки (Кузница/Разблокировки/Аватары/⭐ Эмблемы). Сейв: ленивый дефолт `meta.archIcons={}` — старые сейвы совместимы.
- 📌 На потом (запрошено Артёмом): «вознесение» архетипа — 8-й уникальный апгрейд + премиум-эффект за золото.

## 2026-07-05 — Дотюн урона архетипов + мобильная адаптация экрана

- **⚔ Переход в архетип больше не теряет урон**: пер-архетип множитель `dmgMul` в данных `ARCHETYPES`, читается в `_dmg` (поверх `archM`). По запросу: Берсерк **×2**, Страж **×1.5**, Ловчий **×1.5**. Замер до/после (ST/AoE, real20): Берсерк 1475/2108→**3010/6449** (обгоняет верного мечника 1912/7346 по боссу), Страж 1172/4026→**1759/5142** (паритет по ST + танк), Ловчий 2958/3479→**4649/5397** (обгоняет верного лучника 3929 по ST). Инфо-панель тренировки (`drawTestInfo` D()) теперь показывает урон с учётом archM и dmgMul (была занижена).
- **📱 Мобильная адаптация (для GitHub Pages)**: добавлен отсутствовавший `<meta viewport>` (без него мобилки рендерили в 980px) + app-capable/theme-color мета. Медиазапрос `(pointer: coarse), (max-width:820px)` — канвас letterbox'ится на весь экран (`min(100dvw, 177.7dvh)`), десктоп-подсказка скрыта, рамка/тень убраны. В портрете на телефоне — оверлей «Поверни телефон» (16:9 в портрете мелкий). Тач/мышь и так мапят координаты через `getBoundingClientRect` — letterbox не ломает управление. Проверено inspect'ом: 800×375 → канвас 666×375 на всю высоту.

## 2026-07-04 (3) — Баланс-патч по эмпирическим замерам DPS + скилл /balance-classes

Снапшот до: `versions/index_2026-07-04_pre-balance.html`. Замеры: 68с честной ротации по бессмертным манекенам (цель-босс/пак 6), до и после — одним харнессом.

- **🐞 Мили не доставала до боссов**: попадание считалось по дистанции ЦЕНТРОВ — фикс `+e.r` в ЛКМ мечника и `archCone` (Берсерк/Страж/Дуэлянт/Шквал). Мечник L1: 49→120 DPS.
- **⚖ Архетипы были в 2–10× слабее «верных классу»** (лямбды не читают поля дерева): новый множитель **`archM = 1+0.08×(узлов дерева), кап 2.4`** — фиксируется при выборе архетипа, применяется в `_dmg`. Сила прокачки до 15 ур. теперь конвертируется в силу архетипа. Тюнинг всей системы — одна константа 0.08.
- **⚖ Зоны (hazards) бьют честно**: вместо «яд через Math.max» (зоны не стакались, без крита/урона-боссам/лута/лога) — периодический `e.hit()` раз в 0.5с с атрибуцией src. Чинит Ловчего и все зонные апгрейды.
- **⚖ Ульты усилены** (доля X была 0.5–7%): мечник-нова 2.2→6.0 (r220, стан 1с), маг метеоры 3.6→5.0, лучник залп 3.6→4.5 (pierce 3; 6.0/p4 перекачивал — взрыв на каждом пробитии), Аркан X 4.0→5.5, Стихии X 2.0→2.8.
- **⚖ Берсерк** (был слабее танка): Q 2.8→3.4 (волны 1.2), E конус 57°→92° и 3.0 (лава 0.5), R 2.8 (КД 10→8), X 5.5. **Страж**: второй X-взрыв 3.2→2.2 (танк не лидирует). **Ловчий**: волк 1.1→1.7 (медведь 2.6, вожаки 3.0), R-КД 11→8, стая 1.5 (жизнь 18с), Q-зона 0.8. **Пассив мага** (давал 43–46% урона): T1 4.5→3.0, радиус T2 ×2→×1.6.
- **Итог (ST/AoE, real20)**: Берсерк 373/836→1475/2108 · Страж 340/980→1172/4026 · Дуэлянт 774/2125→2076/6092 · Стихии 805/3925→2181/10827 · Аркан 860/8565→2807/24695 · Призыватель 2318/2816→5581/6628 · Снайпер 1651/4826→3899/11564 · Ловчий 936/1202→2958/3479 · Шквал 759/5678→1854/13764. Верные: мечник 2022/7333, маг 3998/14301, лучник 4007/24151. L1-спред классов ×5.7→×1.9. ARCH_INFO синхронизирован.
- **🛠 Новый проектный скилл `.claude/skills/balance-classes/`** — методика+харнесс+эталоны+точки тюнинга для будущих баланс-итераций (по просьбе Артёма). Наблюдать в плейтесте: Призыватель (топ-ST 5581 на стенде — в живом бою петам мешает движение), AoE лучника/Аркана (рикошет-артефакт стенда).

## 2026-07-04 (2) — Мега-партия: баг-фикс + 14 задач (аватары, иконки, тренировка v3, апгрейды ×7, берсерк-бафф…)

Снапшот до: `versions/index_2026-07-04_pre-megabatch.html`. Тяжёлые блоки спроектированы воркфлоу (4 дизайнера), внедрены агентами, партия прошла адверсариальное ревью агентами.

- **🐞 Баг-фикс «босс за картой»**: клэмп позиций врагов во ВСЕХ точках прямого сдвига (нокбэк archAoe/конуса, таран Стража, толчок Шквала, волна голема-миньона) + клэмп в stun-ветке Enemy.update (главная дыра: стан пропускал общий клэмп — стан+отброс выталкивали боссов за стену).
- **❤ Шкала здоровья**: редизайн — крупная (182×22) px-шкала с фаской/сегментами 20%, пиксель-сердце (краснеет при низком ОЗ), полоска щита сверху; цифры ВСЕГДА целые, шрифт 14px жирный белый с тёмной обводкой (читается на любом цвете).
- **🙂 50 аватаров героя**: процедурные детальные эмблемы (Albion-стиль): шлемы/короны/черепа/звери/оружие/стихии/руны — 23 мотива-рисовальщика по 8–20 фигур, слоёный рендер + кэш в offscreen. 5 дефолтных + 45 за ЖЁСТКИЕ пороги (5k/15k/50k/100k убийств, 50/150 боссов, волны 30/50, угроза 20, 10× Голем-Владыка, 8 уников, 40/60 достижений…). Вкладка «Аватары» в кузнице (сетка 10×5, 🔒+условие), выбранный — в HUD (вместо кружка с буквой) и в меню. `meta.avatar`.
- **🎨 Иконки предметов по грейду** (`drawItemIcon`): процедурные детальные иконки 5 слотов (шлем с гребнем/кираса с наплечниками/сапог/меч с гардой/кольцо с камнем), детализация растёт с редкостью: серый контур → окантовка → орнамент+самоцвет → двойная рамка+свечение → ореол+вращающиеся лучи+искры (Божественный); уник — оранжевое пламя+⭐. **Дропы в мире крупнее** (30–34px вместо ромба 12px) с пышным свечением и ярким широким лучом. Иконки в кузнице/инвентаре/HUD-полосе.
- **🧪 Тренировка v3 — инструмент баланса**: панель всех апгрейдов класса/архетипа с чекбоксами (вкл/выкл через пересборку героя с req-цепочками: авто-добор/авто-снятие зависимых), тумблер «⚡ 0 КД» + сброс КД, live DPS-метр (окно 5с, урон по слотам Q/E/R/X/пассив, Σ с кнопкой сброса, убийства), босс-манекен 25000 ОЗ (без лута). Панели всегда видимы (не убираются клавишами). Манекены не звереют и не дают босс-лут.
- **⬆ Правило разблокировок умений**: Q с 1 ур. / E на 3 / R на 5 / X на 10 (архетип-X на 20); **апгрейды закрытых умений не предлагаются** (фильтр карт по слоту в triggerLevelup + slot:'x' у ульта-карт архетипов). Ранняя прокачка 1–5 замедлена ~×1.5 (`xpForLevel`: 90+50·L^1.22).
- **💢 Бафф «берсерк» монстров**: не умер за 30с (босс — 60с) → каждые 10с стак (кап 5): +10% урона, +10% ск. атаки, +20% размера; кровавая аура — красный тинт спрайта (lighten-градиент), пульсирующее кольцо, кружащие огоньки, взлетающие красные искры, бейдж 💢×N; стак = кольцо+вспышка+рык. В тренировке отключён.
- **🔥 Лава-арена жёстче**: урон 20/0.4с (было 12), зоны каждые 0.9с (было 1.5) и 35% прямо под героем, **зоны взрываются, истекая** (18 урона + кольцо), доп. мобы чаще (2.4с) и усилены ×1.5 ОЗ/×1.3 урона.
- **⚔ Апгрейды до 7 на архетип** (+18 новых механик, 63 карты): кровавые копья по кольцу, афтершоки ульты, руны вдоль рывка Дуэлянта + синхронный резонанс росчерков, лавина глыб Стража + каменные трещины пассивки, кометы Стихий (огонь/лёд поочерёдно) + мега-звезда, метки+сингулярность Аркана, новы духов + воскрешение врагов духами (Призыватель), расщепление+громовая стрела Снайпера, соколиный шторм + тотем-жало Ловчего, спираль 10 волн + зеркальный циклон Шквала. **Пассив-цепочки классов до 6 узлов**: мечник (эхо кольца/выжженная земля/жатва), маг (раскол/лава/двойная комета авто-метеора), лучник (разрывные/рикошет/кольцо смерти).
- **🏠 Меню**: лор в px-плашке + живая фигурка героя (дышит, шагает, машет оружием; класс сменяется каждые 4с) на светящемся постаменте.
- **🎵 Хор «ааа» заменён** на экшн-слой: пульсирующие detune-synth-стабы через lowpass+тремоло (драйв без резкости), верхний акцент — квинта.
- **⚒ Кузница**: предпросмотр заточки — у каждого стата «тек → след» (жёлтым), у уников «сила ×1.X → ×1.Y», подсказка стоимости.
- **🔍 Ревью агентами (3 направления + адверсариальная верификация, 6 фиксов)**: рейдж-размер от пер-инстансной базы (мини-слизни не раздувались бы больше родителя); рейдж копится только в бою (роумеры вне агро не звереют «вхолостую»); аура — каллинг вьюпортом + плоская заливка вместо градиента + без shadowBlur (перф при 200+ мобах); троттлинг SFX 'boss' (700мс); **track/trackMax гейтятся в testroom** (босс-манекен — не ферма достижений/аватаров); панель тренировки не перекрывает HP-бар; trRebuild переносит взятые генерики (`player.genPicks`) и per-run экипировку (замеры DPS честные).

## 2026-07-04 — Партия 7 фич: туториал · Древо Зари · испытания · уники боссов · порталы Бездны · итоги забега · голоса питомцев/боссов

Снапшот до: `versions/index_2026-07-04_pre-7features.html`. Дизайн — воркфлоу из 7 параллельных проектировщиков + интегратор.

1. **🎓 Онбординг/туториал** — интерактивное обучение первого забега (5 шагов: движение → 2 убийства ЛКМ → умение Q → подбор предмета с жёлтой стрелкой и гарантированным дропом → инвентарь I), px-панель поверх боя без паузы, Esc — пропустить. Флаг `meta.tutorialDone`; только аркада и не на таче. Плюс одноразовые контекст-подсказки `meta.tips` (первый дроп / 15 ур. архетипа / первая веха) — золотые плашки сверху.
2. **🌳 Древо Зари** — мета-прокачка за золото между забегами (кнопка в меню, `state='skilltree'`): 15 узлов × 3 ветви (Мощь/Стойкость/Фортуна) по 3–5 уровней, цена ×1.6 за уровень (полное древо ≈12 100🪙), гейтинг по уровням предыдущего узла. Эффекты — `applyTree(p)` после перков достижений (реюз полей игрока; «Золотая жила» — множитель в addGold). Сброс с возвратом 70%. Пиксель-карточки с глифами/пипсами/hover-описанием.
3. **📜 Испытания/дейлики** — экран `state='trials'`: 3 ежедневных + 1 недельное задание из пулов 14/4 шаблонов, детерминированная ротация по дате (FNV-1a→mulberry32), прогресс = дельта stats за забег (снапшот в initGame → зачёт в endRun; тренировка не считается). Награды: 100–400🪙 + 🍀 заряд удачи (+1 удача на след. забег, недельное ×2). Бейдж «!» на кнопке меню, таймер до полуночи, подсказка на Game Over.
4. **⭐ Уникальные предметы с боссов** — 8 именных вещей с лором (Клык/Шкура Тролля, Посох/Филактерия Лича, Сапоги Титана, Ледяная корона, Сердце Голема, Око Владыки): фикс. аффиксы + спец-эффект (казнь <12% ОЗ / реген ×3 при низком ОЗ / разломы при убийствах / +2% ОЗ за убийство / иммунитет к замедлению / замедление при попаданиях / отражение 60% урона / +25% золота). Дроп 18% с босса (Голем-Владыка 35%, заряд удачи ×1.5), без дублей; оранжевый луч + ⭐; заточка работает, реролл/редкость запрещены; в инвентаре/кузнице — имя, ★-эффект и лор.
5. **🌀 Арены-события** — после каждой босс-волны аркады портал Бездны (10с, вихрь + стрелка-индикатор): оверлей `state='event'` с выбором: 🔥 Лавовая арена (25с лавовых зон и доп. врагов → ×2 золота вехи + Легендарный предмет) / 💰 Сокровищница (3 предмета + 150🪙, но элита-страж) / ⚖ Алтарь Бездны (пары благословение+проклятие до конца забега: урон↔ОЗ, скорость↔урон, вампиризм↔входящий урон — новое поле `dmgInM`) / «Уйти». `nextWave` теперь конкатенирует (событийные мобы не стираются).
6. **📊 Итоги забега** — на Game Over/Победе: пиксель-бар-диаграмма урона по 7 источникам (ЛКМ/Q/E/R/X/пассив/питомцы, `runStats.dmgBySrc` из Enemy.hit), топ-3 убитых типов, золото/время/уровень/убийства/макс. комбо. Экраны переверстаны (титул выше, кнопки y500).
7. **🔊 Голоса питомцев и боссов** — 8 синтез-звуков: вой волка (глиссандо+вибрато), тявк лисы, рык медведя, клёкот сокола, шёпот духа (bandpass-свип), топот голема — при призыве (троттлинг: 12 духов = 1 звук); рёв босса при спавне (высота по typeId, Голем-Владыка ниже всех) и драм-аккорд смены фазы (`Enemy.setPhase` — единая точка). Не звучит в бестиарии/тренировке. `SFX.play(n,arg)` — обратно совместимо.

Меню: новый ряд y478 «🌳 Древо Зари / 📜 Испытания / 🧪 Тренировка», профиль+золото на y536. Сейв: ленивые дефолты `meta.tree/trials/trialBuff/tutorialDone/tips` (старые сейвы совместимы).
⚠ На решение Артёма (правило PROD/TEST): учитывать ли удачу в PROD-весах редкостей (`rollRarity`) — сейчас заряд удачи в PROD влияет только на шанс уников (×1.5); гарантировать ли «Редкий+» в Сокровищнице в PROD.
Проверено: полный проход туториала по шагам, ротация/прогресс/клейм испытаний, покупки/гейтинг/сброс древа, эквип/снятие/дедуп/дроп/кузница уников, портал→все 3 события→награды, статистика/экраны, 8 звуков, 4 режима по 60 кадров — 0 ошибок, консоль чистая. Скриншоты: древо, испытания, портал, туториал, итоги.

## 2026-07-03 — Архетипы v4 + боссы v2: 45 уникальных апгрейдов, питомцы, смертоносные боссы

Снапшот: `versions/index_2026-07-03_arch-v4-bosses.html`.

**Архетипы — до 5 уникальных апгрейдов каждому (27 → 45), покрыты ульта и пассивка:**
- Берсерк: +Кратер (X: второй взрыв + озеро лавы) · +Испепеление (пассив поджигает). Топорные удары: ЛКМ ×1.3 + slash-дуга + микротряска, Q/E/R подняты, **X ×3.5→×4.5** со столбом света.
- Дуэлянт: +Казнь (X: цели <35% ОЗ ×2.5) · +Квартет (пассив бьёт вторым пакетом). **Длинные выпады** (конус 95→135px), ульта по 8 целям (было 6, ×1.6→×2.2) с **яркими крест-росчерками** на каждом враге + hitstop + вспышка неба.
- Страж: +Эпицентр (X оставляет огненный разлом со станом) · +Резонанс (пассив шире 40% + второе кольцо). **Танк с ДПС**: ЛКМ ×1.25, Q 1.8→2.2, E 1.4→1.9, R 1.5→2.1, X 2.5→3.2, пассив 0.7→0.95.
- Стихии: **дистанционный редизайн** — E «Реакция» (самокаст) → **«Ледяные пики»**: веер 6 взрывающихся сосулек (r70, замедление); пассивка «огонь/лёд вокруг себя» → **огненный шар с неба** по ближайшему врагу (r85, столб пламени). +Обратный луч (R бьёт и позади) · +Двойной шар (пассив по 2 целям).
- Аркан: **фикс зацикленных сфер** — самонаводящиеся болты ЛКМ детонируют по дистанции (560px, взрыв r48), не кружат вокруг мобов вечно. +Тройные оковы (E ставит 2 доп. печати) · +Цепной разлом (пассив прыгает к следующему врагу).
- Призыватель: **VFX умений** — круги призыва/кольца на каждом духе, столбы света у голема/фамильяра. +Обсидиановый голем (R: ×1.6, ударная волна) · +Исход (X: 12 духов, каждый выходит со взрывом).
- Снайпер: самонаводки E тоже детонируют по дистанции. +Шторм рикошетов (R: 9 стрел с рикошетом во врагов) · +Дуплет (пассив-крит по 2 целям).
- Ловчий: **капкан выкладывается ПЕРЕД героем** (120px), стан 1.6с. +Вожак-медведь (R) · +Сокол (пассив призывает сокола на 4с).
- Шквал: **мобильность** — Q «в упор» даёт отскок назад 80px (кайт), КД рывка E 8→6с; **стрелы ярко взрываются** (Q r55/r95 с апгрейдом, X — кольцо из 14 взрывных стрел + вспышка неба). +Разрывные (Q) · +Попутный ветер (пассив: +15% скорости на 2с, поле `player.windT`).

**Облики питомцев (`Minion.draw` по видам, `kind`):** 🐺 волк (серый, уши/хвост/бег), 🦊 лиса (рыжая, пышный хвост с белым кончиком), 🐻 медведь (крупный, горб), 🦅 сокол (машущие крылья, клюв), 👻 дух (язычок пламени с хвостом и глазами), 🗿 голем (каменный, светящиеся руны). Свои статы: скорость/КД/дистанция по виду. Легаси `'beast'` → волк.

**Базовые ульты всех классов — ярче и мощнее:** мечник — щит гнева теперь БЬЁТ (нова r170 ×2.2 + стан 0.4с + столб + вспышка неба); волшебник — метеоры ×3→×3.6 + вспышка; лучник — залп ×3→×3.6, взрыв 65→85.

**Боссы v2:**
- **График аркады:** Голем-Владыка дебютирует на **15-й волне** (дальше каждая 15-я); на 10-й — обычный босс из четвёрки. ХП подняты на ступень: тролль 1820→2400, лич 2200→2900, титан 3000→3900, владыка 2400→3100, Голем-Владыка 11000→14000.
- **+5 общих умений каждому боссу** (ротация `bk/bkStep` в `Enemy.update`): радиальный залп снарядов · сотрясение-телеграф · призыв свиты · зона смерти под героем · скачок к герою (мили) / прицельный веер (рэйндж).
- **Эскалация смертоносности:** <50% ОЗ — чаще (×0.78), залп 7→10, сотрясение шире (r230), открывается зона смерти; <20% ОЗ — ещё чаще (×0.55), залп 14, урон сотрясения ×1.5, зона смерти r130.
- **Мили-боссы мобильны:** тролль и титан получили скачок-удар (телеграф-пунктир 0.5с → рывок ×3.8 скорости → удар ×1.3 при контакте); у Голем-Владыки остался свой рывок.

Проверено: 9 архетипов × все 5 апгрейдов × полный каст (0 ошибок); 5 боссов × 3 фазы ХП по 6–8с (залпы/призывы/телеграфы живут); скачок тролля 400→159px; зона смерти на <50%; сферы Аркана умирают по дистанции; отрисовка всех 7 видов миньонов. Скриншоты: стая Ловчего, Голем-Владыка ф.3, ульта Дуэлянта.

## 2026-06-19 — Большой редизайн UI: широкий экран 1280×720, пиксель-стиль, большая аркада

Снапшот до: `versions/index_2026-06-19_pre-ui-redesign.html` (жирная партия — точка отката обязательна).

- **Широкий экран 1280×720 (16:9)** вместо 800×600: канвас/CSS (`image-rendering:pixelated`), **UI-слой**: все меню-экраны остаются свёрстанными в 800×600 и рендерятся по центру с масштабом ×1.2 (`uiBegin/uiEnd`, мышь конвертируется на входе в UI-состояниях — все хит-боксы кликов не тронуты, нулевая регрессия). По бокам — пиксельные пилоны (`drawPxGutters`).
- **Универсальная камера** (`updateCam`): мир больше экрана → follow+clamp (аркада/опенворлд); меньше → фикс-центр с тёмным окружением (подземелье/тренировка). Translate применяется во всех режимах.
- **Аркада ~×2.9 больше: 1400×1000** с камерой за героем; арена/сетка/стены/факелы (по периметру каждые 380px)/декор параметризованы под размер. **Хаотичный спавн** (`spawnPos`): каждая тварь — случайная точка у случайной грани с сильным джиттером (не ближе 300 к герою), вместо 8 фиксированных точек.
- **Пиксельная дизайн-система PX (тёмный мир, чёткие края):** `pxPanel` (панель со срезанными углами-ступеньками) + `pxRivets` (заклёпки) + **20+ пиксель-глифов 7×7** (`PXG`/`drawPx`: меч/волна/ярость/щит/рывок/метеор/лёд/цепь/дождь/стрела/яд/кольцо/воронка/крит/вампиризм…). Общий `btn()` рестайлнут → все кнопки всех меню автоматически в пиксель-стиле.
- **Слоты умений в HUD**: вместо тонких полосок — 5 квадратных пиксель-слотов 44×44 (Q/E/R/X + пассивка): глиф умения по классу, цвет архетипа, **КД-заливка сверху + секунды**, бейдж клавиши, замок с уровнем, вспышка готовности, счётчик ×3 пассивки квадратиками.
- **Дизайн-система иконок апгрейдов** (`upgGlyph`): id-префикс карты → пиксель-глиф+цвет (волна синяя, ярость золотая, метеор оранжевый, яд зелёный, уникальные архетипа — звезда в цвете архетипа, генерик-пул — по типу стата). Карточки левелапа и архетипов — пиксель-панели с заклёпками и глифом 49×49 + мелкий эмодзи-бейдж.
- **HUD переразложен под 16:9**: нижняя панель у нижнего края, волновая панель у правого, тосты справа, босс-бар/анонсы/DEV по центру VW; виньетки/флэши/летербокс/пауза/стрелки угроз/онбординг — на весь новый экран. Фоны (градиент/туманности/искры/сетка) следуют за камерой.
- **Мобильная адаптация**: тач-кнопки умений крупнее (r34–38, тап-таргеты ≥60px) в правом-нижнем углу 1280×720, шрифт кнопок больше, подсказка стика у нижнего края. UI-экраны и так масштабируются канвасом.
- Проверено: 5 прогонов (аркада×2/опенворлд/подземелье/тренировка) по 100 кадров с умениями — ошибок нет; все экраны (меню/выбор/достижения/кузница/бестиарий/левелап/архетипы) рендерятся через UI-слой; тач-режим ок. Скриншоты: широкая аркада с камерой, пиксель-HUD со слотами, карточки с глифами, меню.

## 2026-06-19 — Аудио: ветер Бездны → лютня менестреля + боевой рифф

По фидбэку — шумовой «ветер Бездны» заменён на два **мелодичных** слоя (тот же синтез-движок, тональность через `deg()`):

- **🪕 Лютня менестреля** (Witcher-вайб) — в затишье/меню/победе: тёплый щипковый перебор аккорда восьмыми (triangle + тихий октавный обертон, ламповый lowpass, эхо-дилей), классический паттерн «корень-квинта-терция-квинта-октава…» + редкий форшлаг-украшение. Громче всего в затишье (`lu:.085`).
- **🎸 Боевой рифф** (Tekken-драйв) — в бою/боссе: пульсирующий шестнадцатый остинато вокруг корня аккорда (пила с резко закрывающимся фильтром = панчовый щипок), пауза-синкопы, акценты на долях; на боссе громче (`rf:.09`). Вместе с бас-пульсом и киком даёт файтинговый драйв.
- Переход бой↔затишье плавный (кросс-фейд состояний как раньше): рифф глохнет — вступает лютня. Новые голоса `lute()`/`pluckRiff()`, ключи `lu/rf` в MIX; ветер (`windBP/windG`, таблица WV) удалён полностью.
- Проверено: init OK, прогон планировщика combat→boss→calm→menu ~5с без ошибок, настройка mute восстановлена.

## 2026-06-19 — Уникальные апгрейды архетипов (27 шт.) + эскалация графики (5 итераций)

После 15 ур. левелап теперь предлагает **уникальные механические апгрейды выбранного архетипа** (до 2 карт приоритетом + добор из генерик-пула; каждая уникальная берётся один раз). По 3 на каждый из 9 архетипов = **27 уникальных улучшений**:

- **Берсерк:** ✖Крест-накрест (Q добавляет 4 диагональные волны+росчерки) · 🌋Извержение (E — серия отложенных взрывов вдоль трещины) · ⭕Горящее кольцо (R окружает героя 8 очагами огня).
- **Дуэлянт:** 👥Два фантома (Q бьёт зеркально по бокам) · 🗡Залп вперёд (E + веер из 5 клинков) · ⏳Эхо контратаки (R повторяется через 0.5с).
- **Страж:** 🔨Слэм (Q завершается ударом со станом и столбом света) · 📌Шиповая волна (E выстреливает 8 шипов) · 🧱Стена щитов (R ставит 3 стопорящие зоны).
- **Стихии:** ☀Малое солнце (Q: воронка+огонь в точке взрыва) · ❄Вторая волна (E-эхо шире) · 🌋Лава и лёд (X: +3 снаряда с зонами лавы/льда).
- **Аркан:** 🪤Руна-ловушка (Q оставляет взведённую руну на старте) · 🕳Схлопывание (R: дыра взрывается в конце + скайлайт) · 🌀Двойной коллапс (X ×2).
- **Призыватель:** 💥Жертвенный взрыв (призывы взрываются, истекая) · 🌊Ударная волна (удары голема/зверей с отбросом) · 💚Связь душ (атаки призывов лечат).
- **Снайпер:** 🔥Тлеющая линия (Q оставляет огонь по траектории) · 💀**Метка смерти** (E метит 3 целей: +35% урона, пульсирующий череп над головой) · ☄Двойная комета (X повторяется + круговой залп).
- **Ловчий:** 🐍Ползучий яд (зоны ползут за врагами!) · 🪃Подброс (E: доп. стан+разброс) · 🐺Вожаки стаи (X: +2 крупных зверя).
- **Шквал:** ↩Возврат бури (E: герой проносится обратно с уроном) · 🛡Отражение (R уничтожает вражеские снаряды) · 💥Нова-нокбэк (X: финальный взрыв-отброс).

**Инфраструктура:** флаги `p.au{}` + центральный пост-каст хук `archPostCast(p,slot)` (вызывается диспетчером после умения; читает позицию до каста `_pcx/_pcy` для возвратов) + **система отложенных эффектов** `delayedFX[]/after(t,fn)` (афтершоки/эхо/вторые удары). Расширения: `Enemy.markT` (метка смерти: ×1.35 урона + 💀-глиф), Minion-хуки (взрыв/волна/лечение), ползучие hazard-зоны.

**Эскалация графики:** новый вид частиц `slash` (светящиеся росчерки клинков), **вращающиеся спирали** у всех воронок-зон, **тлеющие угли/испарения** над огненными и ядовитыми зонами, двойная ударная волна (белое+цветное кольцо, lighter) на взрывах r≥190, пульсирующий череп метки.

Тренировка: U берёт и уникальные апгрейды, инфо-панель показывает «🃏уник. N/3». Проверено: 9 архетипов × все 3 апгрейда × полный прогон 3.5с (все отложенные сработали) + микс карт на левелапе — ошибок нет.

## 2026-06-19 — Мега-партия: редизайн деревьев, Albion-VFX, декор арен, аудио-слои, уровни аркады

Дизайн разработан параллельным воркфлоу (3 агента: деревья/VFX/декор+аудио), реализация и проверка — в основной сессии.

### 🌳 Редизайн деревьев апгрейдов (меньше статов, больше механик)
- **Удалено 11 «общих стат» узлов** (у каждого класса осталось ≤2 генериков): мечник −s_atk1/s_cd1/s_dmg2/s_ls · волшебник −w_cd1/w_dmg2 · лучник −a_hp1/a_cd1/a_crit1/a_crit2/a_dmg2.
- **Добавлено 18 механических узлов** (реюз hazards/Proj-флагов/explodeAt/slow/poison/stunT):
  - Мечник: **Рывок E — новая цепочка ×4** (🔥огненный след → ⚔взрывы вдоль пути → 💥ударный выход со станом → ⚡второй заряд с перезарядкой по КД) · Волна ❄замедляет · Ярость 🌋разлом-зона.
  - Волшебник: Метеор 🌋лавовое озеро → ☄раскол на 3 · Стужа ❄осколки-снаряды → 🧊обморожение-DoT · Молния ⚡электрозона → 💥громовой раскат на звеньях.
  - Лучник: Шквал ↩рикошет во врагов → ↩↩двойной · Глаз 💥разрывной → 😵паралич · Ливень ☠ядовитое облако → 🌀воронка.
- Новые поля Player/Proj (`dashFire/dashBlastPath/dashExitBlast/dashMax/dashCur`, `waveSlow/furyQuake`, `meteorLava/meteorSplit/frostShards/frostBite/chainZone/chainBoom`, `squallRico/eagleBlast/eagleStun/rainPoison/rainVortex`; Proj: `slowHit/stunHit/rico/lava/split` + `projDetonateExtras`). Инфо-панель тренировки видит новые узлы (префикс `sdash` добавлен в карту).

### ✨ Albion-VFX
- **Рунный круг каста** под героем на КАЖДОМ Q/E/R/X (двойное кольцо+спицы+дуги, вращается, цвет умения; масштаб Q/E<R<X) — `castFX[]`+`spawnCastRune`, слой земли.
- **Новые виды частиц**: `pillar` (столб света на тяжёлых AoE, кап 3), `arc` (каскад расходящихся полуколец у конусных ударов), `flash` (бело-цветное ядро взрыва, composite lighter).
- **Скайлайт**: полноэкранный отблеск на взрывах r≥190 (`skyFlashT`).

### 🏛 Декорации арен (в палитре биома, детерминированно, ноль состояния)
- **Аркада**: 6 рунных менгиров по периметру (пульсирующие руны+glow), обломки колонн у углов, кости/черепа, светящиеся трещины Бездны.
- **Открытый мир**: сетка 240px с хеш-пропсами (рисуются только видимые) — монолиты с рунами, мёртвые деревья, руины стен, рёбра исполина, кристаллы-светляки, озерца Бездны + редкие жаровни-маяки (ориентиры). Перф: 3.8 мс/кадр при 40 врагах.

### 🎵 Аудио-фон
- **Бас-пульс** (пила→lowpass) — драйв в combat (синкопа) и boss (галоп root/квинта), **колокольчики** — редкие «небесные» ноты в затишье/меню/победе (новые ключи `bs/bl` в MIX).
- **Ветер Бездны** — шумовой амбиент через bandpass с LFO-«завыванием» 0.07 Гц; громче в затишье/lowhp, тише в бою; **частота тонируется биомом** (Лёд свистит выше, Зной гудит ниже).
- **Фанфара перехода уровня** (`MUSIC.onLevelTrans`): 3 ноты вверх + колокол + кик — синхронно с баннером.

### 🎬 Уровни аркады
- **Уровень = блок 5 волн** (совпадает с вехами/биомами). На границе — **кинематографичный переход**: белая вспышка → летербокс → «УРОВЕНЬ N — <Биом>» + подзаголовок (усложнение вехи, золото, рост статов); анонсы скрыты на время титула. HUD: «Ур.N · Волна W».
- **Заметный рост сложности**: ОЗ монстров ×1.25 и урон ×1.12 за уровень (поверх волнового роста; капы подняты). Гоблин: ур.1=35 ОЗ → ур.2=61 → ур.3=98.
- Проверено: 18 комбинаций (3 класса × [база с полным деревом + архетип] × 3 режима) + полный U-цикл деревьев (33/38/34 узлов, сирот нет) — ошибок в консоли нет; скриншоты VFX/декора/перехода.

## 2026-06-19 — 🧪 Тренировка v2: все апгрейды за уровни + инфо-панель умений

- **Горячие клавиши прокачки** (только в тренировке): **L** — +1 уровень → обычная карточка апгрейда (после исчерпания базового дерева — генерик-пул); **U** — взять **все** апгрейды разом (база: всё дерево класса с разрешением req-цепочек, тост «Применено N»; архетип: полный сет из 10 карт пула).
- **📖 Инфо-панель умений** (Tab, включена с порога): для текущего класса/архетипа по каждому слоту ЛКМ/Q/E/R/X/Пассив — название, **что делает и как** (описание с механикой: радиусы, станы, DoT, втягивание), **живой урон** (пересчитывается от `dmgM`/полей апгрейдов — видно, как апгрейды меняют цифры), КД с учётом снижений, **счётчик апгрейдов «взято/всего»** по дереву (карта `TREE_SLOT_PREFIX`: Q мечника 9 узлов, R 7, X 2 и т.д.; у рывка честно «нет апгрейдов»; у архетипов — пометка «пул апгрейдов»). Заголовок — сводка статов: база/множитель урона/крит/вампиризм/пробитие/AoE.
- Данные умений архетипов — `ARCH_INFO` (описание + множитель урона; константы синхронны с `ARCHETYPES`). HUD-панель тренировки обновлена: «K класс · L +уровень · U все апгрейды · Tab инфо».
- Проверено: U-база = 31/31 узла мечника применены (dmgM вырос), L даёт карточку (после полного дерева — из пула), U-архетип качает урон и AoE, панель рисуется для базы (со счётчиками) и архетипа (с описаниями), консоль чистая; скриншоты обеих панелей.

## 2026-06-19 — 🧪 Тренировочная комната (тест классов и архетипов)

- **Новый режим `gameMode='testroom'`** — кнопка «🧪 Тренировка (тест классов)» в главном меню (под строкой профиля). Задуман как быстрый полигон для теста классов/архетипов без гринда.
- **Вход:** выбор класса → сразу **20-й уровень** (+95 ОЗ как при честной прокачке) → **экран выбора архетипа с порога** (или «остаться верным классу»). X доступен немедленно.
- **Манекены:** 6 обычных врагов melee-типов (гоблин/скелет/голем/тролль/Проклятый) с **нулевым уроном и скоростью** — стоят и принимают удары; убитые мгновенно заменяются новыми (`spawnDummy`, поддержка count=6 в `gameUpdate`). Герой не может умереть.
- **Клавиша K** (в бою тренировки) — вернуться к выбору класса → заново выбрать класс и архетип (полный цикл перетеста). HUD-панель справа-сверху: «🧪 Тренировка · Манекены не бьют · K — сменить класс».
- Реализовано аддитивно: ветки `testroom` в `initGame`/`gameUpdate`/`drawHUD`/keydown + хит-тест кнопки меню; остальные режимы не тронуты. Проверено: вход/архетип/бой/респавн/несмертность/K — ошибок нет, скриншоты меню и боя в комнате.

## 2026-06-19 — Архетипы v2: оружие, свои пассивки, яркость, опция «остаться в классе»

Доработка системы архетипов по запросу (без переделки базовых механик — всё та же аддитивная схема поверх диспетчера).

- **Своё оружие у каждого архетипа** (рисуется в `drawHeroBody`, атаки красятся в цвет оружия через `accent=arch.wcol` — след маха/кромка/тетива): Берсерк — **боевой топор** с раскалённой кромкой; Дуэлянт — **рапира** (тонкий длинный клинок, циан); Страж — **шипастая булава** (сталь); Стихии — посох со спутниками огня и льда; Аркан — посох с «глазом пустоты»; Призыватель — посох с кружащими духами; Снайпер — **золотой лук** с золотой жилой; Ловчий — оливковый лук; Шквал — лук **с клинками на концах**. Поля `weapon`/`wcol` в контракте архетипа; без архетипа — прежний облик (ветка `base`).
- **Своя пассивка ×3 ЛКМ у каждого архетипа** (заменяет классовую — диспетчер в `triggerPassive`; тиры пассивки из базового дерева продолжают её усиливать через `tm=1+0.3·(tier−1)`): 💥Кровавый взрыв (вампиризм ×2) · 👥Фантомные клинки в цель · 🛡Волна с отбросом и станом · 🔥❄Чередование огонь/лёд · 🌀Мини-разлом у цели · 👻Призыв духа (кап 10 миньонов) · 🎯Гарантированный крит-выстрел · ☠Яд + лужа у цели · 🌪Веер режущих волн. HUD-панель пассивки показывает имя пассивки архетипа.
- **Опция «Остаться верным классу»** — кнопка внизу экрана выбора (15 ур.): архетип не берётся (`archetype='none'`), базовые умения/облик/дерево сохраняются, экран повторно не показывается, карта за 15-й уровень выдаётся из базового дерева; когда базовое дерево исчерпано — генерик-пул апгрейдов (без арх-специфичных карт, флаг `archOnly`).
- **Ярче взрывы/удары:** `archAoe` — двойное кольцо (цвет+белая вспышка), белые искры, звук взрыва при r≥150, микро-хитстоп при r≥190; `archCone` — белые искры + кольцо-вспышка при попадании, сильнее тряска. Названия пула апгрейдов сделаны нейтральными («+22% Урон»).
- **Буст урона где было мало:** ЛКМ Дуэлянта ×1→×1.2, Стихий ×1.3→×1.45, Аркана ×1→×1.15, Призывателя ×1→×1.15, Ловчего ×1→×1.2, Шквала ×1.2→×1.35; призывы: духи ×0.3→×0.42, фамильяр ×0.6→×0.8, голем ×1.2→×1.5, армия ×0.7→×0.85, зверь ×0.9→×1.1, стая ×1.0→×1.15.
- Проверено: 9 архетипов × 9 ударов (3 срабатывания каждой пассивки — счётчики волн/миньонов/зон сходятся) + Q/E/R/X без ошибок; сценарий «остаться»: карта из базового дерева, база работает, повторного экрана нет, fallback-пул после исчерпания дерева; скриншоты 9 видов оружия и экрана выбора. Консоль чистая.

## 2026-06-19 — Архетипы: порт из Godot-версии (9 подклассов, 45 умений)

По запросу — перенос системы архетипов из Godot-ремейка (`Avalo_Godot/CLASSES_AND_ARCHETYPES.md`, реализовано там 19.06) в HTML5-версию. Задача явно поставлена как «не переделывать текущие механики» — весь функционал добавлен **аддитивно**, диспетчер-паттерном поверх существующих методов, без единой правки базовой логики классов уровней 1–14.

- **На 15 ур.** — экран выбора 1 из 3 архетипов класса (`state='archsel'`, необратимо на забег). ЛКМ/Q/E/R заменяются полностью; **X — с 20 ур.** Диспетчер в начале `attack/qAbility/eAbility/rAbility/xAbility`: если `player.arch` задан — вызов `arch.*(p,projs,parts[,combo])` и `return`, вся текущая (немного другая по числам, но неизменная) логика класса — ниже, не тронута. Регрессия для игроков без архетипа исключена структурно (early-return branch).
- **9 подклассов × 5 умений = 45 новых способностей:**
  - Мечник: 🔥Берсерк (Гнев маятника/Раскол земли/Кровавый вихрь/Удар титана), ⚔Дуэлянт (Каскад клинка/Фантомный рывок/Вихрь-клинки/Парад-контр/Тысяча росчерков), 🛡Страж (Таранный рывок/Шипастый оплот/Щит-таран/Сейсмический таран).
  - Волшебник: 🔥Стихии (Ледяное копьё/Огненный шар/Реакция стихий/Стихийная буря/Армагеддон), 🌀Аркан (Арканный залп/Арканная вспышка/Печать оков/Чёрная дыра/Коллапс реальности), 👻Призыватель (Рой духов/Дух-фамильяр/Призыв голема/Армия предков — новый `class Minion`).
  - Лучник: 🎯Снайпер (Шквал стрел/Пронзающий выстрел/Соколиный глаз/Стрела-рикошет/Стрела-комета), 🐾Ловчий (Ядовитая роса/Капкан/Ловчий питомец/Великая охота), 🌀Шквал (Вихрь копья/Стрела в упор/Шквал-рывок/Танец древка/Буря клинков и стрел).
- **Новая инфраструктура (переиспользуемая, не дублирующая):** `archAoe`/`archCone` — генерик-хелперы AoE/конус-урона (реюз `e.hit/score/lifesteal/rings/particles`); `hazards[]`+`spawnHazard()` — ally-owned зоны яд/лава/вортекс (бьют врагов, реюз `enemy.poison/poisonT/stunT`, в отличие от `groundFX`, которые бьют игрока); `class Minion`+`minions[]` — дружественные призывы (spirit/golem/beast, атака ближайшего врага, живут по таймеру, урон не получают — упрощение).
- **Апгрейды после 15 ур.** — генерик-пул `ARCH_UPGRADE_POOL` (10 карточек: урон/AoE/крит/КД/пробитие/вампиризм/ОЗ/скорость/ск.атаки/защита), репитабл (без `req`-цепочек), **100% реюз существующих полей игрока** (`dmgM,critBonus,qCdM…xCdM,pierce,lifesteal,maxHp,spd,atkCdM,dmgReduction` — только `archAoeM` новое поле).
- **`xpForLevel`**: уровни 1–14 — прежняя формула (без изменений/регрессий); с 15 — экспоненциальный хвост (жёсткий поздний прогресс, первая прикидка).
- **HUD/облик:** герой перекрашивается в цвет архетипа (`drawHeroBody`); бейдж уровня + тег названия архетипа; X-слот показывает имя умения архетипа и лочится по Lv20.
- **Сознательные упрощения контроля объёма** (см. `CLAUDE.md`): апгрейды — генерик-пул вместо 45 бесповских «путей»/форков из Godot-доки; R волшебника «Стихийная буря» — каст-луч, не true hold-channel; «рут»/«капкан» реюзают `enemy.stunT` (полный стан).
- Проверено: 27/27 комбинаций (9 архетипов × 3 режима: аркада/опенворлд/подземелье) — атака+3-комбо+Q/E/R/X+урон+движение, 90 кадров каждая, **ошибок в консоли нет**. Полный левелап-конвейер 1→15(экран архетипов)→выбор→20(разблокировка X) проверен пошагово. Документация: `CLASS_DESIGN.html` (аддендум), `CLAUDE.md` (новый раздел «Архетипы»).

## 2026-06-19 — Гуманоидный облик 3 классов героя (порт из sprite_lab)

Откат: git (`git checkout index.html`) · снапшот после — `versions/index_2026-06-19_hero-look.html`. Дизайн разработан и утверждён в песочнице `sprite_lab/hero_designs.html` (2 итерации, отдельно от игры) + загрузчик PNG `sprite_lab/_save_server.py` для скриншотов. Портирован в `Player.draw` через **try/catch с фолбэком** на старый круг — баг в облике не может сломать игру.

- **Герой больше не «цветной шар с 2 глазами» — теперь фигура:** голова (шлем с гребнем/визором у мечника · остроконечный капюшон с тёмной лицевой впадиной у мага/лучника), торс с каймой, две руки (плечо→кисть-перчатка с локтём), две ноги с сапогами (шагают при ходьбе), у мага/лучника — развевающаяся мантия. Модуль `drawHeroBody` + хелперы (`drawLegs/drawRobe/drawTorso/drawArm/drawHead/drawSwordsman/drawWizard/drawArcher`), рисует в локальной системе `forward=+x` (поворачивается за прицелом).
- **Детальные движения оружия:**
  - **Мечник** — широкий стальной клинок с золотой гардой/навершием + круглый щит в левой руке; **мах**: клинок проносится дугой через корпус ( easing) + яркий бело-голубой след-трейл.
  - **Волшебник** — посох в двух руках, на навершии «рога» оправы и **яркий пульсирующий орб**; при атаке посох даёт отдачу, орб вспыхивает в огонь + кружат искры.
  - **Лучник** — лук **полукругом во внешнюю сторону** (выгиб от героя), **тетива к герою**, наложенная стрела смотрит вперёд (по ТЗ); при выстреле тетива снапит вперёд + вспышка спуска; колчан за плечом.
- **Яркость умений:** при Q/E/R/X — цветной светящийся ореол вокруг героя (`castAnim`/`castCol` по классу+слоту) поверх уже существующих частиц.
- **Анимация жизни:** дыхание в покое, шаг (фаза/бобинг) при движении, отдача при атаке. Новые поля игрока: `atkAnim/castAnim/castCol/walkPhase/moving/breath` (тикают в `Player.update`, ставятся в `attack`/`qAbility`/`eAbility`/`rAbility`/`xAbility`).
- **Экран выбора класса** теперь показывает живую фигуру класса (вместо плоской иконки ⚔/✦/◈); при наведении — периодический мах/выстрел.
- Проверено в игре (frame-pump + скриншоты): 3 класса рисуются в бою во всех направлениях (вниз/влево/вверх — оружие следует за прицелом, лук всегда выгибом наружу), атака/каст дают яркие FX, ошибок в консоли нет, экран выбора рендерит фигуры.

### Доработка (2026-06-19, та же партия)
- **3-шаговое комбо ЛКМ** (`comboStep`→`atkCombo`, цикл 0→1→2→0; **только анимация, урон/тайминг те же**): **Мечник** — 1) форхенд, 2) бэкхенд, 3) широкий разрубающий финишер с яркими золотыми искрами (+кольцо/шейк/звук); **Волшебник** — взмах посохом 1) в одну сторону, 2) в другую (со следом-дугой орба), 3) занос над головой → «отправка метеора» вперёд (струя из орба + орб ярче); **Лучник** — 1 и 2 обычные, 3) глубокий натяг тетивы с задержкой (`atkAnim` 0.34 вместо 0.2) и мощный спуск (большая вспышка + линии скорости). Длительность анимации обобщена через `atkAnimMax` (норм. `ar`).
- **Фикс хит-флэша монстров/боссов:** при ударе больше **не подсвечивается старый силуэт** (`drawEnemyShape` белым) — раньше новый спрайт гасился условием `flash<=0`. Теперь **новый спрайт рисуется всегда**, а попадание — яркая белая вспышка (радиальный градиент, `lighten`) поверх него. Старый рендер остаётся только как try/catch-фолбэк при сбое спрайта.

### Полишинг-автопилот (2026-06-19, серия итераций по запросу «улучшай дальше»)
- **Iter 1 — фигуры героев живее:** походка-подскок (масштаб-сквош), наклон корпуса в сторону бега (`moveLX/moveLY` — сглаженный локальный вектор движения), покачивание плеч в шаге (`rk`), шире шаг + подъём стопы, сильнее биллоу плаща, дыхание/качание оружия в покое. Баг-свип: 3 класса × аркада/опенворлд/подземелье × атаки+комбо+Q/E/R/X+урон (150 кадров) + все экраны (меню/выбор/достижения/кузница/бестиарий/левелап/гейровер) — **ошибок в консоли нет**.
- **Iter 2 — отдача врага при ударе:** при попадании враг визуально отлетает от источника (`flinchX/flinchY`, затухание ~0.3с; чафф 7.5 / элита 4.5 / босс 2.5 px) — обёрнуто в `save/translate/restore` вокруг тела в `Enemy.draw` (save только при ненулевой отдаче — перф). **Чисто визуально**, позиция/ИИ не меняются.
- **Iter 3 — афтеримедж рывка мечника:** при E-рывке вдоль траектории остаются 7 голубых полупрозрачных силуэтов героя (`dashGhosts`, рендер `drawHeroBody` с альфой перед `player.draw`, затухание 0.32с).
- **Iter 4 — магнит лута + луч редкости:** дроп в радиусе подбора+95px плавно тянется к герою; у предметов Необычный+ — светящийся столб цвета редкости над ромбом (`Drop.update/draw`).
- **Iter 5 — стрелки к внеэкранным угрозам:** в открытом мире боссы/элиты вне экрана отмечаются пульсирующей стрелкой у края (цвет по роли, крупнее для боссов) — `drawThreatArrows()` в screen-space.
- **Iter 6 — низкое ОЗ + флэш урона:** при HP<25% — пульсирующая красная виньетка; при получении урона — короткий красный флэш по экрану (`dmgFlash` в `takeDamage`, затухает).
- **Iter 7 — индикация готовности умений:** при сходе Q/E/R/X с КД полоска в HUD вспыхивает контуром в цвет умения (`qRf/eRf/rRf/xRf` — фронт КД→готово в `Player.update`).
- **Iter 8 — онбординг:** первые ~7с забега снизу-центра подсказки управления (WASD/мышь/ЛКМ-комбо/Q-E-R-X), плавно затухают (`onboardT`). Скрыты на тач-управлении.
- **Iter 9 — трейлы снарядов:** Метеор (Q) и авто-метеор (пассив) получили огненный хвост (`fireTrail`); базовый трейл снарядов чуть ярче.
- **Iter 10 — перф + баг-свип:** полный прогон 3 класса × 3 режима × умения/комбо/урон/толпа (160 кадров) — ошибок нет; при норм. плотности (40 врагов) ~2.7 мс/кадр; отдача врага оптимизирована (save только при отдаче). Высокая плотность по-прежнему гасит свечение (`LITE`>70).

## 2026-06-17 — Редизайн облика монстров (порт из sprite_lab)

Откат: git (`git checkout index.html`, до коммита 026a083) · снапшот после — `versions/index_2026-06-17_enemy-sprites.html`. Дизайны разработаны и утверждены в песочнице `sprite_lab/` (4 итерации, отдельно от игры, без конфликтов с правкой) и портированы в `Enemy.draw`.

- **Все 26 типов врагов получили проработанный силуэт** вместо «шар + 2 точки-глаза»: рядовые (гоблин-зубастик, череп-скелет, баньши с лохмотьями, голем из плит, носорог-таран, дух-маг, тёмный маг, жаба-плеватель, фантом с двойниками, призыватель с рунным кругом, жрец с нимбом, взрывун с фитилём, слизень с дочерними каплями), элиты (рыцарь со щитом к герою, чумной в облаке заразы, берсерк, зеркало, аватар Бездны, кристалл-голем, жнец с косой), боссы с фазами (тролль, лич с короной/щитом, титан с расколом, владыка холода, Голем-Владыка с короной/молниями).
- **Движок:** модуль `MSPR` (рисовальщики тел) + `MFX` (частицы). Каждый спрайт читает состояние врага: ярость (ОЗ<34% → сдвиг в красное), фаза босса (`phase`), атака (`tgT`/`chState`/`swingT` → телеграф-поза), глаза следят за героем. Вызов в `Enemy.draw` обёрнут в **try/catch с фолбэком** на старый рендер — баг в спрайте не может сломать игру.
- **Анимации жизни:** материализация при спавне (`spawnT` → fade+scale-in + кольцо), эмбиент-частицы у ключевых типов (искры фитиля взрывуна, ихор чумного, дым жнеца/призрака, искры-блики кристалла/владыки — с капом по числу частиц), и **распад при смерти** по типу (`MFX.death`): гиб/осыпание костей/взрыв/прах/раскол стекла/ядовитый выброс.
- **Класс `Particle` расширен** (обратно совместимо): опц. гравитация + виды `shard`/`smoke`/`spark`/`ring` (+ glow/rot/grow/drag). Существующие вызовы не затронуты.
- Проверено frame-pump'ом: скрипт парсится, игра грузится (`MSPR` 26 типов), спрайты рисуются (пиксели = цвета дизайнов), смерть даёт частицы `ring/shard/smoke/spark`, эмбиент идёт, ошибок в консоли нет. Скриншот сцены со всеми 25 типами + мега-боссом — корректный рендер с HUD/маркерами/полосами ОЗ.
- **Плейтест/перф (балансная правка):** замер на толпе 150 врагов выявил перерасход на `shadowBlur` (свечение). Фикс: `eye()` ставит блюр только при свечении (раньше блюрил и неглящиеся глаза — впустую у каждого врага), + режим `LITE` (при >70 врагах свечение отключается). После фикса спрайты дешевле старого рендера (×0.81 на 150 врагах); при норм. плотности (40) ~14 мс/кадр. Ярость (ОЗ<34%) даёт явный красный сдвиг (гоблин: зелёный→`rgb(166,91,35)`).

## 2026-06-15 (XV) — Ангельский хор + стан Ярости + красочные биомы

Снапшот до: `versions/index_2026-06-15_pre-choir-color.html`.

- **🎵 Эпичный хор (ангельские голоса)** в `MUSIC`: новый слой `choir` — формантный синтез «ааа» (пила → 2 параллельных bandpass на формантах 700/1150 Гц = вокальный тембр) + медленное вибрато + тяжёлый reverb-send («собор»), 3 голоса (триада на 2 октавы выше) + высокий shimmer-голос ещё октавой выше. Громкость по состояниям (`MIX.ch`): тихо в меню/затишье, **громко и торжественно на боссах (.10) и победе (.12)** — медленно вплывает (эпично). В тональности (через `deg()`), глушится тумблером M.
- **⚔️ Ярость мечника — настоящий стан:** апгрейд `sfur6` теперь **оглушает на 1.5с** (`furyStun=1.5`) и применяет реальный `enemy.stunT` (враг стоит и не атакует), а не замедление. Вторичная волна (`furyShock`) — стан 0.6×.
- **🎨 Красочный фон (биомы):** палитра фона меняется по вехам/тиру (`BIOMES`: Багровая Бездна / Мёртвый Лёд / Гнилые Топи / Пустота / Пепельный Зной) — анимированный вертикальный градиент + дрейфующие туманности (радиальные градиенты) + мерцающие искры + сетка в цвет биома; работает и в Аркаде/Подземелье, и в Открытом мире. Добавлена **виньетка** (затемнение краёв) для глубины/фокуса.
- Проверено frame-pump'ом: 9 комбинаций класс×режим + меню без ошибок; хор строится без ошибок, Ярость даёт `stunT=1.5`, биом-фон рендерится.

## 2026-06-15 (XIV) — Золото только за вехи + лесенка усложнений + боевые аффиксы

Снапшоты: до — `versions/index_2026-06-15_pre-gold-affix.html`.

### 💰 Золото — только за вехи (+ эскалация монстров)
- **Все прежние источники золота убраны** (за волну, за босса +120, за Проклятого +40, за комнату подземелья, за тик угрозы). Золото теперь **только за вехи**.
- **Вехи — каждые 5 волн** (Аркада) / **каждые 5 уровней угрозы** (Открытый мир) / **за босса-этаж** (Подземелье). Выплата `120 + тир·70` (веха 1 = 190🪙, 2 = 260, …); в подземелье — за финального босса.
- **После вехи — резкое усиление монстров (фиксированная лесенка `MILE_LADDER`, накопительно):** 1 ⚡Ускорение(+12% спид) · 2 🛡Закалка(+22% ОЗ) · 3 💢Ярость(+18% урон) · 4 ✦Обереги(щиты 12% ОЗ у чаффа) · 5 ☠Взрывная кровь(чафф взрывается) · 6 ⚡Ускорение II · 7 ➕Регенерация · 8 👹Полчища(+40% плотность); дальше — общая эскалация статов. Модификаторы — глобальный `MODS`, применяются в конструкторе `Enemy` (ХП/спид/урон) и в поведении (щит/взрыв/реген/плотность).
- **HUD:** «🔺 Веха N» + иконки активных модификаторов (левая панель); анонс при вехе с названием нового усложнения. Сброс вех в `initGame`.

### 🗡 Аффиксы снаряжения — боевой уклон + перебаланс
Меньше плоской защиты (она урезана прошлым ребалансом), больше урона/крита/скорости/утилиты:
- **Голова:** крит / КД умений / **урон** / опыт (убран КД ульты).
- **Корпус:** ОЗ / броня / **урон** / вампиризм (убраны щит и реген — самые слабые при новых капах).
- **Ноги:** скорость / **ск.атаки** / рывок / уклонение (убрана неуязвимость).
- **Оружие:** урон↑ / ск.атаки↑ / **крит** / пробитие / урон-боссам↑ / лёд / огонь.
- **Кольцо:** крит / урон↑ / **ск.атаки** / удача / вампиризм (убран ОЗ — кольцо = офенс).
- Числа офенс-аффиксов подняты (~+30%), защитные — в рамках новых капов.
- Проверено frame-pump'ом: золото идёт только за вехи (убийство босса = 0🪙), веха 5 → +190🪙 + MODS.spd 1.12, монстры после вехи спавнятся усиленными, веха 10 → +ОЗ; 9 комбинаций класс×режим со всеми модификаторами активными — без ошибок.

## 2026-06-15 (XIII) — Адаптивная музыка + ребаланс «Телеграфная летальность»

Снапшоты: до — `versions/index_2026-06-15_pre-music-rebalance.html`; после — `versions/index_2026-06-15_music-rebalance.html`. Дизайн собран многоагентным воркфлоу (7 агентов: 3 философии ребаланса + 2 подхода к музыке → синтез).

### 🎵 Адаптивная процедурная музыка (модуль `MUSIC`)
- 100% синтез Web Audio, **без ассетов**, переиспользует AudioContext модуля `SFX` (один граф; тумблер `M` глушит и музыку, и эффекты; состояние в `meta.muted`).
- **Слои:** суб-дрон (root+октава), пэд-триада, арпеджио (через фид-бэк-дилей «пещера»), лид-мотив «тема Авало», кик, хэт, пещерный шумовой бэд. Громкость музыки ≤0.12 — всегда под эффектами.
- **Тональность:** D-эолийский (меню/затишье/бой/победа), D-фригийский для боссов (мрачный b2). Прогрессия i–VI–III–VII; у боссов Dm–Eb–Dm–Am; победа — пикардийский подъём к D-мажору.
- **Адаптивность (lookahead-планировщик 25мс):** состояния `menu / calm / combat / boss / low-HP / victory / gameover` с кросс-фейдами слоёв, лерпом BPM (64→138) и открытием мастер-lowpass по интенсивности (`tension = плотность врагов + близость`). Low-HP — оверлей: сердцебиение (учащается к смерти), всё глушится. Босс — ризер+стинг при появлении.
- **Хуки:** `ensure()` по жесту, `tick()` каждый кадр (определяет состояние), `onBoss/onCalm/onGameOver/onVictory/onThreatRise/duckForLevelup`.

### ⚔️ Ребаланс «Телеграфная летальность» (сложно, но честно)
**Телеграфы (новый контракт честности):** тяжёлые AoE боссов больше не бьют в тот же кадр — у них **windup-окно** (красный круг + растущий жёлтый радиус = «ударит через миг»), общий механизм `enemy.tg(r,mult,dur,...)`. Тролль-слэм 0.6с, землетрясение Титана 0.7с, ледяная нова Владыки 0.8с (урон 1.2→1.5x), землетрясение Голем-Владыки 0.8с. КД разнесены, чтобы телеграфы не накладывались (тролль 2.5→3.0, титан 2.6→3.2, голем 3.5→4.0). Зоны заморозки/тьмы взводятся 0.4–0.5с; Жнец — кольцо при старте замаха (0.7с); раскат-стан Голем-Владыки 200→165px; щит Лича — окно неуязвимости фикс 2.0с.
**Сломанное усмирено:** взрыв щита мечника x4→**x1.6** + только по таймеру (не при пробитии) + щит 45→40%; вихрь x5→x4, база Ярости x6→x4, дуга 1.22→1.0 (cap 1.7→1.45), стан 2→1.2с, вампиризм мечника 0.15→0.08; капстоун метеора волшебника переведён с мультипликативного радиуса на аддитивный (115·1.7·1.3·1.4≈356 → 115+55+30+40=240).
**Выживаемость подрезана системно:** кап брони 0.85→0.55, реген 0.6→0.35/с, кап вампиризма 0.12→0.08, кап уклонения 0.75→0.45, дроп 0.05→0.045, кривая опыта 38·lvl^1.25→40·lvl^1.28. **i-frames (0.5с) намеренно сохранены** — основа честности.
**Враги — угроза через урон, не «губки»:** глобальный рост урона по волнам 1.032→**1.045** (софт-кап ×3.2); сильнее бьют гоблин/скелет/баньши/плеватель, чаще стреляют касты; рывок Носорога x1.6→x2.2, взрыв Взрывуна x1.8→x2.5, Проклятый 3640→2800 hp / 40→50 dmg. **Элиты: 3640→2400 hp, но +~25% урона** (стеклянная пушка), спавнятся с 8-й волны и растущим числом (1→4). Голем-Владыка 15000→**11000** (единственная намеренная «стена»). Плотность ↑ (больше чаффа, кап 210→240).
**Контрплей-баффы:** i-frames рывка мечника 0.3→0.45, КД заморозки волшебника 11→9, рывок лучника 9→8 + скорость 185→190.
**Классы (различимы и живучи):** мечник 150/38→**140/42** (рисковый дайвер), волшебник 80/48→**72/54** (хрупкий нюкер), лучник 105/22→**98/26** + скорость (мобильный кайтер, снайпер 0.67→0.75).
- Проверено frame-pump'ом: 9 комбинаций класс×режим + меню без ошибок; телеграф откладывает урон (~0.7с подтверждено), новые статы/HP, музыка во всех состояниях — ок.

## 2026-06-15 (XII) — Звук (Web Audio) + мобильное управление

Снапшот до партии: `versions/index_2026-06-15_pre-sound-mobile.html`.

- **Звук** (модуль `SFX`): синтез через Web Audio (без ассетов), негромкий и мягкий — lowpass-фильтры + компрессор, master-громкость 0.4, короткие огибающие. События: атака/выстрел, попадание, крит, смерть врага, урон по герою, умения Q/E/R/X, левелап, подбор, золото, волна, босс, взрыв, клик UI. Анти-спам (троттлинг по типу звука). Аудио стартует по первому жесту (клик/клавиша/касание). **Выкл/вкл: клавиша `M`** + кнопка 🔊/🔇 в меню; состояние в `meta.muted`.
- **Мобильное управление** (модуль `TOUCH`): авто-включается при первом касании. **Левый стик** — движение (аналоговое), **правый стik** — прицел + огонь; без правого стика — **авто-прицел на ближайшего + автоогонь**. Экранные **кнопки умений Q/E/R/X** (с индикацией КД и блокировкой по уровню). Координаты касаний мапятся как мышь; в меню работает синтетический клик. `drawTouchUI` рисует стики и кнопки.
- Проверено frame-pump'ом: 9 комбинаций класс×режим + меню без ошибок; `SFX.play/toggle`, тач-движение, авто-прицел, кнопки умений, рендер тач-UI — подтверждены.

## 2026-06-15 (XI) — Цифры урона умений, счётчики забега, баланс мечника, масштаб окна

Снапшот до партии: `versions/index_2026-06-15_balance-ui.html`.

- **Масштаб окна** уменьшен: `width: min(85vw, 113vh)` (было `100vw/133.333vh`).
- **Цифры урона умений (+критические):** все попадания умений (Q/E/R/X + пассив + волны клинка) показывают всплывающее число; крит — белым с `✦`. Реализовано централизованно в `Enemy.hit(dmg,parts,src,crit)`: `crit='roll'` (умения сами роллят крит ×2.5), `crit=true/false` (база/снаряды). Базовые атаки по-прежнему показывают только крит-числа (без захламления). Хелпер `fmtNum`.
- **Счётчики забега (HUD, лево-верх под бейджем):** ☠ Убито (в т.ч. боссов отдельно) + ⚔ Урон. Поля `runStats.kills/bossKills/dmg`.
- **Баланс мечника:**
  - Пассивка (кольцо удара) −27% (`×1.5`→`×1.1`).
  - **Взрыв щита** (апгрейд «Щит взрыва») сильно усилен: урон ×4 (+300%), радиус 95→**200**, **оглушение 1.5с** поражённым. Новая механика — **`enemy.stunT`** (оглушённый враг не двигается/не атакует; визуал — жёлтое кольцо + ⚡). В `explodeAt` добавлен параметр `stunDur`.
  - **Яростный удар (R):** урон +50% (`rFuryMult` 4→6), радиус 90→**130**.
  - **Волна клинка (Q):** урон +15% (`waveQMult` 2.8→3.22).
- Проверено frame-pump'ом: 9 комбинаций класс×режим без ошибок; крит-цифры умений, оглушение (stunT=1.5, движение=0), счётчики (kills/bossKills/dmg) — подтверждены.

## 2026-06-15 (X) — 7 элитных монстров + смягчение тряски + анимация молнии/стана

Снапшот до партии: `versions/avalo_rpg_2026-06-15_pre-elites.html`.

- **7 новых элитных монстров** (`E_ROLE='elite'`, ХП 3640 — уровень Проклятого; всего **26** типов врагов). Спавн: с 9-й волны Аркады (ротация `ELITES`, 2-я с 16-й) + роумеры Открытого мира с угрозы 5 (редко). Бейдж «ЭЛИТА» в бестиарии — авто.
  - **🛡 Осквернённый рыцарь** — щит спереди блокирует ~82% урона; поворачивается медленно → бить в спину.
  - **☣ Чумной носитель** — ядовитые лужи (DoT) + облако заразы при смерти (`groundFX kind='poison'`).
  - **💢 Берсерк** — rage-скейл: ниже ОЗ → выше скорость и урон (×до ~2.04).
  - **🪞 Зеркальный двойник** — стреляет снарядами в цвет класса героя (`mirrorCol`).
  - **🌑 Аватар Бездны** — ставит зону тьмы (`kind='void'`): нет реген-лечения + урон героя −40% (`player.voidT`).
  - **💎 Кристаллический голем** — отражает 30% дальнего урона обратно (по дистанции >92px, кулдаун).
  - **⚰ Жнец** — телепорт вплотную к герою + широкий замах косы по площади.
- **Смягчение тряски камеры (поправка):** кап `addShake` 16→9; взрывы (огн.шары мага / стрелы лучника) ×0.07→×0.035; криты 2.5→1.2; способности монстров снижены (Титан 11→5, Ледяная нова 10→4.5, землетрясение Владыки 13→6, молния 8→3.5, рывок 5→2.5, slam тролля 9→4.5). Тряска осталась как «такт», но мягкая.
- **Анимация Голем-Владыки:** оглушающий раскат теперь рисует анимированную радиальную молнию (`boltT`, 7 зазубренных лучей). У оглушения героя — визуал: жёлтое пульсирующее кольцо + 4 кружащих ⚡ (`player.stunT`).
- Всё проверено frame-pump'ом: 9 комбинаций класс×режим + бестиарий без ошибок; механики элит (rage 2.04, фронт-блок 18% vs тыл 100%, отражение, зона тьмы блокирует реген + урон ×0.6, ядовитый DoT) и рендер эффектов (пиксель-сэмпл) подтверждены.

## 2026-06-15 (IX) — Ребаланс лута + мега-босс Голем-Владыка + классы монстров

Снапшот до партии: `versions/avalo_rpg_2026-06-15_bosses.html`; после партии: `versions/avalo_rpg_2026-06-15_rebalance-golemlord.html`.

- **Правило прод/тест (процесс):** перед реализацией всегда спрашивать, что идёт в PROD, что в TEST(DEV) — но разделять можно **только** то, что достигается игровым временем: **скорость прокачки** и **шанс/редкость дропа**. Фиксы классов, боссы, анимации, UI, механики — **не разделяем** (едины в обоих режимах). Зафиксировано в `CLAUDE.md` (раздел Dev/Prod).
- **Ребаланс снаряжения (#2):** убраны аффиксы «зона подбора» (head/legs); все статы снаряжения снижены на 50% (`AFFIX_SCALE=0.5`). **PROD-дроп** перебалансирован: 5%/слот (`PROD_DROP`), редкости `PROD_RWEIGHT` — Божественный **0.003%** с мобов; боссы `BOSS_RWEIGHT` — ≥Редкий, Божественный **~1.5%**. **TEST(DEV)** — без изменений (быстрый дроп/высокие редкости). Распределения проверены frame-pump'ом (0.003% / 1.48% / 5%).
- **Мега-босс «👑 Голем-Владыка» (`golemlord`, #3):** ХП 15000 (×5 Голема-Титана). Огненные шары + землетрясение огромного радиуса (r300) + оглушающий раскат молнии (стан героя 1с) + интенсивный рывок к герою каждые 10с + щит 15% ХП каждые 30с (поглощает урон). Появляется **только** на волнах 10/20/… (Аркада) и угрозе 7/14/21 (Открытый мир); **только 1 на карте** во всех режимах. Полоса ХП босса оставлена **только для него** (у остальных боссов убрана). Стан героя: новое поле `player.stunT` (блокирует движение/атаки/способности); щит босса: `eShield`.
- **Бестиарий (#4):** у каждого монстра класс-тег **БОСС / ЭЛИТА / ОБЫЧНЫЙ** (по `E_ROLE`), цвет и фон строки по роли.
- Всего типов врагов: **19** (4 босса + мега-босс).

## 2026-06-15 (VIII) — Реорганизация папки + откат версий + инбокс идей

- **Папка:** роли всех файлов зафиксированы в `CLAUDE.md` (манифест + процедура аудита «проверь организацию папки»). `GAME_PROMPT.md` → `_archive/` (исходный концепт, больше не ведётся). Уточнены роли: `Идеи.txt` — личный инбокс Артёма (не трогаем), `ABOUT_PLAYERS.md` — лор (правки только по запросу), `fun_versions/` — личные снапшоты.
- **Откат версий:** папка `versions/` со снапшотами `avalo_rpg_ГГГГ-ММ-ДД_тег.html` (создаются перед крупной партией, привязаны к CHANGELOG; откат по запросу «верни версию от …»). Первый снапшот: `versions/avalo_rpg_2026-06-15_bosses.html` (состояние после боссов).
- **Инбокс идей:** `PLAYTEST_NOTES.md` — быстрые заметки с тестов по категориям + триаж по команде «разбери идеи».

## 2026-06-15 (VII) — Идея 5: 3 босса + фикс лучника

- **Идея 5 — боссы (реализовано):** 3 новых босса (`boss:true`), всего 18 типов врагов.
  - **Лич** — щит-неуязвимость циклом (блокирует урон, `invuln`) + призыв нежити; фазы по 50/25%.
  - **Голем-Титан** — землетрясение (волны+тряска), раскол на 2 големов при 50%.
  - **Ледяной Владыка** — поля заморозки (`frostZones`, замедляют героя через `player.chillT`); фаза 3 — ледяная нова по экрану.
  - Появление: ротация на босс-волнах (5/10/15/20→ тролль/лич/титан/владыка), редко в открытом мире (угроза ≥8), случайный финал подземелья. Полоса ХП босса сверху, маркеры/аура, гарант. лут ≥Уникальный + 120 золота.
- **Фикс лучника:** убрана способность «Самонаведение» (`a_homing`) — была слишком сильной при множестве стрел; базовый урон лучника снижен на ~20% (28→22). Дерево лучника: 34→33.

## 2026-06-15 (VI) — Зрелищность + дизайн монстров + ТЗ боссов

- **Зрелищность (задача 2):** экранная тряска (`shake`/`addShake`) на взрывах/боссах/критах/уроне; hit-stop (микропауза) на крит-килл и смерть босса; всплывающие числа урона (крит ✦ белым, урон по герою красным); крит ×2.5 (было ×2); импакт-кольца на крит. Ограничения зафиксированы (один файл, без ассетов, 800×600, перф ~200 врагов).
- **Дизайн монстров (задача 3):** роль каждого (`E_ROLE`) → цветной контур+glow (ближний/танк/дальний/поддержка/босс/элита); силуэты-формы (`E_SHAPE`: голем-квадрат, баньши/призрак-капля, фантом-ромб, слизень-приплюснутый); маркеры роли над особыми (💀☠➕✦💣⚡🌀). Бестиарий авто-отражает.
- **Идея 5 — ТЗ готово** (`specs/TZ_05`): 3 босса (Лич, Голем-Титан, Ледяной Владыка), без модификаторов. Реализация — следующий этап.

## 2026-06-15 (V) — Бестиарий 📖

- Экран **«📖 Бестиарий»** из меню (`state='bestiary'`): список всех 15 монстров с реальным артом (рендер через `Enemy.draw`), ролью, характеристиками (ОЗ/Скорость/Урон/Опыт) и описанием механики (`MONSTER_GUIDE`). Прокрутка колесом. Авто-отражает будущие изменения дизайна монстров.

## 2026-06-15 (IV) — Идея 3: Кузница и золото 💰⚒

- **Золото** (`meta.gold`, копится между забегами): начисляется **только за волны/боссов** — волна `8+wave*4` (×3 босс-волна), рост угрозы `10+wave*5`, комната подземелья `15+20·hpMult`, килл босса `+40`. Всплывашка `+N🪙`. В DEV ×3.
- **Персистентный стэш** (сундук, 24 ячейки) + сохраняемая экипировка. Старт забега — из `meta.equipped`; в конце забега найденное уходит в стэш.
- **Кузница** (отдельный экран из меню «⚒ Кузница»): **реролл аффиксов**, **повышение редкости** (+аффикс), **заточка** (`sharpen`, +значения) — за золото. Заточка учитывается в статах (`applyItem`).
- **Разблокировки:** 5-й слот **«Кольцо/Амулет»** (500🪙, свой пул аффиксов) и **косметика-скины** (цвет героя, 200🪙).
- Инвентарь стал адаптивным (поддержка 5-го слота). Добавлен стат `luck` для колец.

## 2026-06-15 (III) — Понятные описания достижений и снаряжения

- **Достижения — ясные карточки:** у каждого достижения теперь показывается **«Цель: …»** простым языком (что делать; `METRIC_DESC` на 74 метрики) и **«🎁 Бонус: <перк> — до +X% за 5 тиров»** (+ пометка «только за <класс>» для классовых). Прогресс-бар показывает «текущее / порог (тир N/5)». Строки выше для читаемости.
- **Инвентарь — понятные слоты:** для каждого слота (Голова/Корпус/Ноги/Оружие) выводятся **возможные бонусы (аффиксы)** и краткая роль слота; сверху — **легенда редкостей** (сколько аффиксов даёт каждая) + правило «выше редкость = больше аффиксов и сильнее значения». Надетый предмет — с редкостью, ур. и значениями.
- **Инвентарь ставит игру на паузу** (клавиша `I`): `gameUpdate` замораживается при `showInv` — можно спокойно изучать характеристики.

## 2026-06-15 (II) — Dev/prod-режим и древо лучника

- **#4 Dev/Prod-режим (✅):** тумблер в меню (⚙ правый верх). DEV — быстрый опыт (`XP_RATE`=10), дроп до 60%/слот, почти всегда высокие редкости; горячие клавиши `−/=` (опыт) и `[ / ]` (лут) + панель в HUD. PROD — обычный баланс. Реализован «лёгкий» вариант, как согласовано.
- **Древо лучника доведено до 34 скиллов** (как у волшебника; было 14): база/стат/пассив расширены (урон, крит ×2, mshot 1→5, снайпер) + апгрейд-цепочки способностей **Q Шквал** `aq1–5`, **E Меткий глаз** `ae1–5`, **R Ливень** `ar1–5`, **X Залп** `ax1`.
- **Новые механики лучника:** ☠ **ядовитые стрелы** (`a_poison` → DoT на враге, `enemy.poison/poisonT`), 🧭 **самонаведение** (`a_homing` → стрелы доворачивают к ближайшему врагу). + взрывные стрелы Шквала/Ливня.
- Обновлены `CLASS_DESIGN.html` (полное древо лучника без пробелов), `CLAUDE.md`.

## 2026-06-15 — Публикация, UX опыта, левелинг, баг-фиксы

**Релиз:** проект опубликован на GitHub Pages — играть: <https://torgart-dacx.github.io/Avalo/> · репозиторий: <https://github.com/torgart-dacx/Avalo>

| # | Изменение |
|---|-----------|
| 1 | **Анонсы боссов убраны** (всплывающее окно — визуальный мусор при многих боссах): нет анонсов фаз тролля и BOSS ROOM. Остались только анонсы волн-вех (5/10/…) и роста угрозы. Для всех режимов. |
| 2 | **Левелинг переработан:** равномерная кривая `xpForLevel(L)=50+38·L^1.25` (нет гипер-старта, есть время изучить умения до ~25 ур.). Опыт врага масштабируется с ХП — Проклятый/боссы дают заметно больше (goblin 10 / troll 164 / cursed 328). |
| 3 | **Визуализация опыта:** всплывающие числа `+N xp` над убитыми (крупнее для боссов); отдельная крупная **шкала опыта сверху-слева** с плавным заполнением и читаемыми цифрами. |
| 5 | **Экран расширен** на всю сайтовую область (масштаб 4:3, внутреннее разрешение 800×600); координаты мыши масштабируются. Идеи мобильной адаптации — в CLAUDE.md (Backlog). |
| 6 | **Баг-фикс:** Q-волна мечника не появлялась в открытом мире — `BladeWave` умирал по экранным границам (880/680). Теперь границы мира. Механики скиллов идентичны во всех режимах. |
| 7 | **Волшебник — ясность метеоров:** разделены и переименованы апгрейды «Метеор Q» / «Метеодождь X» / «Авто-метеор (пассив)». У пассивного метеора свой радиус `passMeteorR` — апгрейды Q больше его не задевают. |
| 8 | **Карта гейм-дизайна классов** `CLASS_DESIGN.html` — визуальное дерево способностей/апгрейдов с уровнями разблокировки. Поддерживать актуальной при изменении скиллов. |
| — | Добавлено: фэнтези-описание (меню + README), `KB.md` (хаб знаний), этот `CHANGELOG.md`. Фэнтези-описание на главном экране. |

**Открытые вопросы (заданы пользователю):** dev/prod-режим (#4); параметры идей 3–5 для старта реализации.

---

## (ранее) — Фундамент и системы

- **База:** 3 класса (Мечник/Волшебник/Лучник), 3 режима (Аркада / Подземелье / Открытый мир 3000×2000), фичи 0.1–0.7 (пассивки, X-слот, крит, ПКМ-апгрейд, босс-фазы, sticky-стрелы, взрыв о стену).
- **F1–F7:** кривая опыта, +5 ОЗ/уровень, урон врагов ×1.032/волну, прыгучие стрелы (2 отскока→застревание), открытый мир + камера, боссы ×7 ХП, Проклятые.
- **Апгрейды+монстры:** 35 апгрейдов способностей (волшебник Q/E/R ×7, мечник Q/R ×7), 7 новых монстров с уникальными механиками во всех режимах, спавн ближе к герою, ×3 монстров.
- **Идея 1:** достижения (75 × 5 тиров) + мета-прогрессия (перки в localStorage).
- **Идея 2:** лут и редкости (6 редкостей, 4 слота, аффиксы, дроп 18.7%/слот ⚠).
- **Документация:** ТЗ идей 3–5 (`specs/`), майнд-карта проекта `MINDMAP.html`.
